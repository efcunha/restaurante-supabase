{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && bash ../docs/scripts/download-latest-eas-builds.sh && bash ../docs/scripts/publish-eas-builds-to-restaurante-site.sh && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve out -s -l $PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
